<div class="comment">
  <div class="comment-head">
    <img src = "{{ 'avatar_blog.png' | asset_url }}" alt="" />
  </div>
  <div class="comment-content">
    <div class="comment-ad">
      <span class="author">{{ comment.author }}</span>
      <span class="date">{{ comment.created_at | date: " %b %d, %Y" }}</span>
    </div>
    {{ comment.content }}
  </div>
</div>

